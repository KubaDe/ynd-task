import { accordionTheme } from '@components/theme/components/accordion';
import { buttonTheme } from '@components/theme/components/button';
import { inputTheme } from '@components/theme/components/input';

const components = {
  Button: buttonTheme,
  Input: inputTheme,
  Accordion: accordionTheme,
};

export default components;
